<% content_for :title, "New job" %>

<div class="min-h-screen bg-[#F8FAFC] p-10">
  <div
    class="
      max-w-4xl mx-auto bg-white rounded-[24px] shadow-sm border border-[#F1F5F9] p-10
    "
  >
    <div class="flex justify-between items-center mb-10">
      <h1 class="text-4xl font-bold text-[#0F172A]">New Job Application</h1>
      <%= link_to jobs_path, class: "text-gray-400 hover:text-gray-600" do %>
        <%= icon_close %>
      <% end %>
    </div>

    <%= render "form", job: @job %>
  </div>
</div>
